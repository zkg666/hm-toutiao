<template>
    <div class='container'>
    <!-- 登录卡片 -->
    <el-card>
      <!-- logo -->
      <img src="../../assets/logo_index.png" width="200px" alt="">
      <!-- 表单 -->
    <el-form ref="form" :model="formlogin">
        <!-- 表单选项 label="活动名称" 表单输入框前的文字-->
        <el-form-item>
        <!-- 表单元素 -->
            <el-input v-model="formlogin.username" placeholder="请输入用户名" v-focus></el-input>
        </el-form-item>
        <el-form-item>
        <!-- 表单元素 -->
            <el-input v-model="formlogin.code" class="code" placeholder="请输入验证码"></el-input>
            <el-button>发送验证码</el-button>
        </el-form-item>
        <!-- 复选框 -->
        <el-checkbox :value="true">我已阅读并同意用户协议和隐私条款</el-checkbox>
        <el-form-item>
            <el-button type="primary" class="btn">登录</el-button>
        </el-form-item>
    </el-form>
    </el-card>
  </div>
</template>

<script>
export default {
  data () {
    return {
      formlogin: {
        username: '',
        code: ''
      }
    }
  },
  directives: {
    'focus': {
      inserted (el) {
        console.log(el)
        el.querySelector('input').focus()
      }
    }
  }
}
</script>

<style scoped lang='less'>
// 全屏容器
.container{
  width: 100%;
  height: 100%;
  // 背景尺寸 background-size 拆分写法
  // 组合写法 background:    / 背景尺寸(width,height) 简写 cover 铺面整个容器多余看不见  contain 等比例缩放完整的显示在容器内容
  background:url(../../assets/login_bg.jpg) no-repeat center / cover;
  position: absolute;
  left: 0;
  top: 0;
}
// 饿了么组件 el-card 组件名称  解析成HTML  后组件根元素上默认生成一个类和组件的名称一致
.el-card{
  width: 400px;
  height: 350px;
  position: absolute;
  left: 50%;
  top:50%;
  transform: translate(-50%,-50%);
  img{
      text-align: center;
      margin-bottom: 20px;
  }
}
.code{
    width: 235px;
    margin-right: 10px;
}
.btn{
    width: 100%;
    margin-top: 30px;
}
</style>
